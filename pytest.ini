[pytest]
testpaths = harness/tests
addopts = -ra --strict-markers
markers =
    integration: tests that require external services or credentials
    slow: tests that intentionally take longer than the default unit suite
