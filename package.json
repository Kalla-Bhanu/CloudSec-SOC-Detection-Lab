{
  "name": "cloudsec-soc-detection-lab",
  "version": "0.0.0",
  "private": true,
  "description": "Static dashboard smoke tests for a synthetic cloud security detection lab.",
  "scripts": {
    "generate:evidence-assets": "node tools/generate-public-evidence-assets.mjs",
    "test:dashboard": "playwright test",
    "verify:evidence-assets": "node tools/generate-public-evidence-assets.mjs --check"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
